// Push Notifications and Data processing
f.open(data_file)
data = f.read()

// Check for changes
if there are changes:
  if data.bin_level == 100:
    // Push status: Bin Full
  else
    // Push status: Bin Level Percentage

  if data.battery_cap <= 10:
    // Push status: Battery Low, %
  else
    // Push status: Battery Ok, %
